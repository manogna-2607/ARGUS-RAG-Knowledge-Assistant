# ARGUS document extraction runtime dependencies
# PyMuPDF is the primary page-aware PDF parser.
PyMuPDF>=1.24,<2.0
# Used for DOCX paragraphs and table extraction.
python-docx>=1.1,<2.0
# PDF parser fallbacks when PyMuPDF is unavailable or rejects an unusual PDF.
pdfplumber>=0.11,<1.0
pypdf>=5.0,<6.0
